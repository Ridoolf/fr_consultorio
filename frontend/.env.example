# Copiá este archivo a .env (solo en tu PC):
#   copy .env.example .env
#
# .env NUNCA se sube a GitHub. En Netlify la variable va en Site settings → Environment.

# Desarrollo local (Vite proxy → http://127.0.0.1:8000)
VITE_API_URL=/api

# Producción (Netlify)
# VITE_API_URL=https://fr-consultorio-backend.onrender.com/api
